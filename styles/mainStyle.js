import { createGlobalStyle } from "styled-components";

export const Global = createGlobalStyle`
    .slick {
        .slick-track {
        display: flex;
    
        .slick-slide {
            display: flex;
            height: auto;
        }
        }
    }
`;

